#include "global_variables.cpp"
#include "parameter.h"

class synth {
    int analog_read() {

        // for (param)

        for (int i=0; i<num_params; i++) {

        }


        return 0;
    }
};